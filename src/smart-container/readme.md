# ContainerBoot

Provides registration, discovery, and lifecycle management of application services. A **service** is any object or function that can perform a task: for instance, the `express` service provides an HTTP server and route handling. Services can be registered directly or loaded via a shared module through **bundles** (which contain **service definitions**).

