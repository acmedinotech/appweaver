{
  "name": "@acmedinotech/smartcontainer",
  "version": "1.0.0-alpha",
  "description": "SmartContainer is a declarative approach to defining, managing, and retrieving services for applications of any size. Inspired by Apache Felix & the OSGi framework spec.",
  "main": "dist/index.mjs",
  "module": "dist/index.js",
  "exports": {
    ".": "./dist/index.mjs"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "lint:types": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.mjs --platform=browser --target=es2020 --format=esm --tsconfig=tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.5.2",
    "esbuild": "^0.25.2",
    "express": "^5.2.1",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
